# Day 6 - 前后端联调与软件开发入门

## 📌 今天学了什么？

今天是培训的第 6 天，内容主要围绕**前后端联合开发**展开。作为一个新手，我之前对“前端”“后端”“接口”这些词只有模糊的概念，今天总算搞清楚了它们是什么、怎么配合工作。

另外我们还学了 **Linux 常用指令**和 **Conda 环境管理**，这些是后续开发中必备的工具技能。

---

## 一、接口与接口文档

### 1.1 什么是接口？（用大白话解释）

**接口**，可以理解成**服务器开了一个“窗口”**，让外部程序（比如网页、App）来跟它沟通。

举个例子：
- 你打开一个外卖 App，看到附近的餐厅列表 —— 这个列表数据是从服务器获取的。
- 你的 App 不能直接翻服务器的数据库（太危险了！），只能通过服务器**专门开放的一个窗口**去要数据。
- 你按照约定好的方式（比如访问某个网址、带上必要的参数）发送请求，服务器收到后处理，再把结果返回给你。

这个“怎么请求、请求什么、返回什么”的约定，就是**接口**。

> 🧠 **我的理解**：接口就像餐厅的“取餐窗口”——你不能进后厨（服务器内部），但可以在窗口点餐（发请求），然后拿到做好的菜（返回结果）。窗口上贴的菜单（点餐方式、菜品编号）就是接口文档。

### 1.2 接口的作用

| 作用 | 解释 |
|------|------|
| **前后端独立开发** | 前端和后端只要约定好接口格式，就可以各自写代码，不用互相等。 |
| **隐藏内部复杂性** | 调用方只需要知道“传什么参数、拿到什么结果”，不需要关心服务器内部怎么实现。 |
| **统一管控与安全** | 所有操作都通过接口，可以集中做身份验证、权限检查。 |
| **一处开发，多处复用** | 同一个接口，网页、App、小程序都能用，不用重复开发。 |

### 1.3 接口文档是什么？

接口文档就是一份**详细的使用说明书**，记录着：

- **接口地址**：要请求的网址（比如 `/api/user/info`）
- **请求方式**：GET（获取数据）、POST（提交数据）、PUT（修改）、DELETE（删除）
- **请求参数**：需要传什么参数、是否必填、什么类型
- **返回结果**：成功或失败时会返回什么数据

有了接口文档，前后端开发人员就不用天天口头确认了，照着文档写就行。

---

## 二、架构介绍与选择

### 2.1 什么是代码架构？

**代码架构**，简单说就是**怎么组织你的代码**。就像盖房子要先有设计图，写代码也要先想好结构。

常见的架构模式：

| 架构模式 | 说明 | 适合场景 |
|----------|------|----------|
| **单体架构** | 所有功能写在一个项目里，统一部署 | 小项目、快速验证 |
| **分层架构** | 按职责分层（比如界面层、业务层、数据层） | 最基础、最常用的模式 |
| **微服务架构** | 拆成多个独立服务，各管一块业务 | 大型复杂系统、高并发 |

选架构主要看三点：**业务复杂度**（功能多不多）、**团队规模**（多少人开发）、**扩展需求**（未来会不会做大）。

> 🧠 **我的理解**：就像搬家打包——东西少可以全塞一个箱子（单体架构），东西多了就要分类装箱（分层架构），东西特别多就要分车装（微服务架构）。

### 2.2 前端是什么？

**前端**就是用户**直接看到和操作**的部分——网页的布局、按钮、颜色、交互效果。

前端三件套：
- **HTML**：定义页面有什么（按钮、输入框、图片）
- **CSS**：控制长什么样（颜色、大小、位置）
- **JavaScript**：处理交互逻辑（点击后做什么、数据怎么刷新）

现代前端开发通常使用框架（如 **React、Vue**），让开发更高效。

### 2.3 后端是什么？

**后端**运行在服务器上，用户看不见，但前端的一切动态内容都靠它支撑。

后端负责：
- **业务逻辑处理**：比如下单时检查库存、计算价格
- **数据存取**：从数据库读写数据
- **接口提供**：向前端开放调用入口
- **安全与权限**：验证身份、控制访问

### 2.4 数据库是什么？

**数据库**就是专门**存储和管理数据**的软件系统。

常见类型：
- **关系型数据库（MySQL、PostgreSQL）**：数据以表格形式存储，适合订单、财务等需要事务一致的场景。
- **非关系型数据库（MongoDB、Redis）**：存储方式灵活，适合缓存、实时统计等场景。

---

## 三、从生产角度进行项目编写

### 3.1 明确需求 + 确定项目结构

今天课上模拟了一个**学生管理系统**的需求，包含：
- 管理学生信息（增删改查）
- 月考、期中、期末成绩管理

技术栈选了：
- **后端**：Python（异步架构）
- **前端**：Electron（后期可扩展到 App 和桌面端）

> 💡 **实操思路**：先用大模型（比如 DeepSeek）帮忙选框架、产接口文档，审核确认后，再把接口文档交给 AI 编程助手（如 Trae）来生成代码。

### 3.2 多 Agent 协同开发

老师建议：**开多个 AI 助手窗口，分工协作**
- 一个负责前端
- 一个负责后端
- 一个负责调试和杂活

这样效率更高，各司其职。

### 3.3 启动与检查

**后端启动**：
```bash
cd student-management-backend
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
**前端启动**：
```bash
cd student-management-frontend
npm install
npm run start   # Electron 模式
```
### 3.4 遇到 Bug 怎么办？
#### 排查顺序：

- 看命令行报错——后端问题会在这里显示

- 前端按 F12 → 控制台（Console）——前端问题在这里看

**🧠 我的经验：报错不可怕，可怕的是不报错但结果不对。这种“静默错误”最难排查，要特别注意。**
## 四、计算机的特性——为什么有了 AI 还要学习？
### 4.1 一个反直觉的例子：-2 > 1 竟然为 true？
```bash
int a = -2;
unsigned int b = 1;
if (a > b) {
    cout << a << endl;   // 竟然输出了 -2！
}
```
**为什么？**
- C++ 在比较有符号数（int）和无符号数（unsigned int）时，会把有符号数转成无符号数再比较。

- -2 转成 32 位无符号整数后变成 4294967294（一个巨大的数），当然大于 1。

- 所以条件成立，进入 if 分支。但输出时 a 仍然是 int 类型，所以打印的还是 -2。
**🧠 我的理解：这就是隐式类型转换的坑！编译器悄悄帮你做了转换，但结果可能完全不符合你的预期。**
 ### 4.2 有了 AI 还需要学习吗？
 **答案是：需要！**
**AI 可以帮你写代码、查报错，但如果你不理解底层原理（比如上面的类型转换规则），遇到这种“不报错但结果不对”的情况，你连问题出在哪都不知道。**
