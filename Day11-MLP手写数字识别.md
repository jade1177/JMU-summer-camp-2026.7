# Day 11-MLP手写数字识别复盘
# 一、今天到底在做什么？一句话理解
**用神经网络（MLP）训练一个模型，让它能识别0-9的手写数字。**
**你不是在学"深度学习原理"，你是在学"怎么跑通一个AI训练流程"。这对AI产品经理来说，就是理解"模型是怎么从数据里学出来的"。**

# 二、核心概念：用"教小孩认字"来理解神经网络
## 2.1 人工智能、机器学习、深度学习的关系
```plain
人工智能（AI）          ← 大目标：让机器像人一样智能
    └── 机器学习（ML）   ← 方法：用数据训练模型，而不是手写规则
            └── 深度学习（DL）← 子集：用多层神经网络，模拟人脑神经元
```

**这个实验属于深度学习，因为用了多层感知机（MLP），有输入层→隐藏层→输出层。**
---
**把过程详细拆解一下**
| 层级 | 职场类比 | 你的实验 |
|------|----------|----------|
| **输入层** | 前台小妹：把收到的快递（图片）拆开，把物品清单（64个像素值）记下来 | 64个像素值 |
| **隐藏层** | 部门主管：看清单后思考——“这尺寸像A类客户，这重量像B类产品”，提炼出32个关键特征 | 32个神经元（提取特征） |
| **输出层** | 总经理：听主管汇报后，最终盖章签字——“这是数字5！” | 10个数字的概率 |
```text
第一步：图片进来，变成了什么？
你有一张8×8的手写数字图片（比如写了个“0”）。

电脑看不懂图片，只认识数字。所以这张图被拆成了64个小格子，每个格子有一个亮度值（0=全黑，16=全白）。

这64个数字，就是输入层。相当于前台小妹收了一张单子，上面写了64个数字，递给后面的员工。

到目前为止：没有任何判断，只是把数字递进去。

第二步：中间员工（隐藏层）在干嘛？
你招了32个中级员工（这就是隐藏层）。他们每人手里拿着一张“秘密偏好菜单”。

重点来了：这32个员工依赖什么数据来判断？

他们只依赖前台递进来的那64个数字（像素值）。

但他们是这么工作的（每个人分工不同）：

员工1：他的菜单上写着“我喜欢左上角黑、中间白”——当他看到这64个数字符合这个规律，他就给自己打个高分（比如8分）。

员工2：他的菜单上写着“我喜欢右边有竖线”——如果符合，他也打个高分（比如9分）。

员工3：他的菜单上写着“我喜欢全是乱的”——如果不符合，他就打低分（比如0分）。

这一步的结果：32个员工，每人报出一个分数（比如 [8, 9, 0, 3, 6, ...] 一共32个分数）。
```
**回答问题：隐藏层依赖什么数据？**

**答：只依赖刚输入的那64个像素值。（菜单是之前训练好的，现在只是照着菜单打分）**
```text
第三步：老板（输出层）怎么拍板？
你是老板（输出层），你手底下有10个盒子（分别代表数字0、1、2、3……9）。

你拿到32个员工的打分报告，你心里也有一本“老板的偏好账本”：

你心想：“员工1打高分时，大概率是数字0；员工2打高分时，大概率是数字1……”

于是你给10个盒子算总分：

“数字0”的盒子得分：2.1分

“数字1”的盒子得分：-1.5分

“数字2”的盒子得分：8.7分（最高！）

现在最关键的一步（百分比怎么来的）：
老板觉得：“直接报8.7分太生硬了，我要把它变成百分比！”
于是老板拿出计算器（这个计算器就叫Softmax），对着这10个分数算了一下，让它们加起来正好等于100%。
最后结果变成：

数字0：1%

数字1：0%

数字2：95%（最高，就选它！）

数字3：4%

```

**回答问题：输出层依赖什么得出结果？**
**答：依赖隐藏层32个员工报上来的分数，加上老板自己的偏好账本，算完后再用计算器掰成百分比。**
### 第四步（你最困惑的）：员工和老板手里的“菜单/账本”从哪来？
**上面说的“员工喜欢看什么”、“老板偏好什么”，这些就是模型里的权重。**
```text
在你第一次运行程序的时候，这些菜单和账本全是乱写的（随机数）！
所以第一次猜，模型纯属瞎蒙，准确率只有80%。

那么它们是怎么变准的？依赖什么数据？

依赖 “真实答案”（也就是你训练集里的标签）。

举个例子（训练过程）：

你给模型看一张“8”的图片。

员工瞎打分，老板瞎算，最后老板说：“我猜这是数字3，概率60%！”

这时候，正确答案（数字8）跳出来了，系统说：“老板，你猜错了！”

系统开始倒着追责：

先骂老板：“你的账本不对！下次看到这些员工分数，要更偏向‘8’的盒子！”

再骂员工：“你们几个的喜好菜单不对！下次看这张图，你们应该打出不同的分！”

每次骂完，员工和老板就微微修改自己的菜单和账本（这就是学习）。

把1797张图反复看30遍，被骂了无数次后，菜单和账本终于被修改得非常准了。
```
## 2.2 神经网络是什么？
### 类比：教一个从来没见过数字的小孩认0-9
| 神经网络组件                 | 小孩认字类比                         |
| ---------------------- | ------------------------------ |
| **输入层（64个像素）**         | 小孩眼睛看到的图片                      |
| **隐藏层（32个神经元）**        | 小孩大脑里的"特征提取"：这个有圆圈可能是0，有竖线可能是1 |
| **输出层（10个类别）**         | 小孩说出口的答案："这是3"                 |
| **权重（Weights）**        | 小孩大脑里"圆圈=0"这个连接的强弱             |
| **偏置（Bias）**           | 小孩认字的"基础门槛"，比如看到什么程度才敢说"这是0"   |
| **激活函数（ReLU/Sigmoid）** | 小孩的判断标准：看到特征后，要不要"兴奋"（激活）      |
| **损失函数**               | 答错了，老师给的"惩罚分数"                 |
| **反向传播**               | 答错了，老师告诉小孩"你哪里想错了"，小孩调整思路      |
| **训练（Epoch）**          | 把练习题反复做很多遍                     |
## 2.3 我的实验数据：MNIST（简化版）
| 属性   | 数值            | 说明                       |
| ---- | ------------- | ------------------------ |
| 图片数量 | 1797张         | 比真实MNIST（60000张）小很多，适合入门 |
| 图片尺寸 | 8×8像素         | 真实MNIST是28×28，这里更简化      |
| 像素值  | 0-16          | 0=黑，16=白，中间是灰            |
| 类别   | 0-9           | 10个数字                    |
| 划分   | 80%训练 / 20%测试 | 1437张训练，360张测试           |
**为什么图片要"展平"成64个数字？**
8×8的图片 → 按行展开 → 64个数字的向量
```text
原始图片（8×8）:          展平后（64个数字）:
[5, 0, 0, 0, 0, 0, 0, 5]  →  [5, 0, 0, 0, 0, 0, 0, 5, 0, 12, ...]
[0, 12, 8, 0, 0, 8, 12, 0]
```
**MLP只能处理"一列数字"，不能处理"二维图片"。这是MLP的局限性，也是为什么图像任务后来用CNN（卷积神经网络）——CNN能直接看二维图片。**
## 2.4 网络结构：64 → 32 → 10
```plain
输入层（64个神经元）      隐藏层（32个神经元）       输出层（10个神经元）
    [像素1]                   [神经元1]                [数字0的概率]
    [像素2]                   [神经元2]                [数字1的概率]
    [...]        →           [...]         →          [...]
    [像素64]                  [神经元32]               [数字9的概率]
    
    每个像素连接到           每个隐藏神经元连接          Softmax把输出变成
    每个隐藏神经元            每个输出神经元              概率（加起来=1）
```
**参数计算：**
- 输入→隐藏：64×32个权重 + 32个偏置 = 2080
- 隐藏→输出：32×10个权重 + 10个偏置 = 330
- 总共：2410个可训练参数
## 三、实验流程复盘
### 3.1 环境准备
| 步骤                            | 你做了什么                    | 为什么               |
| ----------------------------- | ------------------------ | ----------------- |
| 解压文件到 `D:\lab\MLP_Digits_Lab` | 创建项目文件夹                  | 路径不含中文和空格，避免报错    |
| VS Code打开文件夹                  | 不是打开单个文件                 | 保持项目结构完整          |
| 新建终端                          | Terminal → New Terminal  | 在正确目录下执行命令        |
| 创建虚拟环境 `.venv`                | `py -3.12 -m venv .venv` | 隔离依赖，避免污染系统Python |
| 安装依赖                          | 用离线wheels安装              | 不需要联网，避免版本冲突      |
| 检查环境                          | `check_env.py`           | 确认所有库都装好了         |
### 3.2 运行训练
```bash
.\.venv\Scripts\python.exe train_mlp.py \
  --learning-rate 0.05 \
  --hidden 32 \
  --activation relu \
  --epochs 30 \
  --output results\baseline
```
**参数解释：**
| 参数                          | 值        | 作用                          |
| --------------------------- | -------- | --------------------------- |
| `--learning-rate 0.05`      | 0.05     | 每次更新参数的"步长"。太大容易震荡，太小学得太慢   |
| `--hidden 32`               | 32       | 隐藏层有32个神经元。越多=模型越复杂=越容易过拟合  |
| `--activation relu`         | ReLU     | 激活函数：负数变0，正数不变。计算简单，梯度不容易消失 |
| `--epochs 30`               | 30       | 把训练集完整看30遍                  |
| `--output results\baseline` | baseline | 结果保存位置                      |

### 3.3 结果解读
**01_samples.png：数据样本**
- 显示0-9的手写数字样本
- 图片很小（8×8），边缘有锯齿，这是正常的
- 产品经理视角：理解"模型输入长什么样"，才能评估"这个输入质量够不够好"
**02_curves.png：训练曲线**
- 左图（Loss）：损失从1.87降到0.04，说明模型在学
- 右图（Accuracy）：
- 蓝线（Train）：从80%升到98.8%
- 橙线（Test）：从81.7%升到95.8%
- 差距：训练准确率比测试高3%，轻微过拟合，但不严重
  
#### 关键问题：为什么前5轮变化最快？
- 初始参数随机，模型最先学到"容易区分"的特征（比如0是圆圈、1是竖线），所以进步快。后面学的都是"细枝末节"。
#### 关键问题：为什么损失还在降，准确率却不变了？**
- 准确率只看"猜对没"，损失还看"猜得多有把握"。从70%信心提升到95%信心，准确率不变，但损失会降。

#### 03_confusion_matrix.png：混淆矩阵
- 对角线=猜对的
- 数字8最惨：35个真实8里，只有26个猜对，4个猜成1，3个猜成7，2个猜成9
- 产品经理视角：知道模型"哪里容易犯错"，才能设计产品方案（比如让用户多写几遍8）
  
#### 04_mistakes.png：错分样本
- 看这些图片，你会发现：有些数字本身就很模糊
- True 9 / Pred 4：这个9写得像4，人也容易认错
- 产品经理视角：错误不一定来自模型差，可能来自"输入质量差"
# 四.产品经理视角：你今天学到了什么
| 概念             | 产品经理怎么用                            |
| -------------- | ---------------------------------- |
| **训练集/测试集划分**  | 评估模型时，必须看"没见过的新数据"表现，不能只看训练数据      |
| **过拟合**        | 模型"死记硬背"训练数据，遇到新数据就懵。产品上线前要检测      |
| **混淆矩阵**       | 知道模型在哪些场景下容易犯错，设计兜底策略（比如低置信度时提示用户） |
| **准确率不是唯一指标**  | 95%准确率意味着5%错误，如果产品是医疗诊断，这5%可能是致命的  |
| **数据质量决定模型上限** | 8×8的模糊图片，人眼都容易认错，模型再强也有天花板         |
